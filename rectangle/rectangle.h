#ifndef RECTANGLE_H
#define RECTANGLE_H

#include <cmath>

long double rectangle (double, double, long);
long double sum (long, long double, double, long);

inline long double func ( long double x ){

	return x;
}

#endif // TRAPEZOIDA_H
